///播放器管理类
class PlayerManager {
  ///当前是否可显示
  static bool canShowRecommendDialog = true;

  ///是否显示对话框(VIP和购买)
  static bool hasFollowShowDialog = false;

  ///当前是否可显示
  static bool canShowFollowDialog = true;

  static bool hasSecondShowDialog = false;

  ///当前是否可显示对话框
  static bool canShowSecondDialog = true;
}
